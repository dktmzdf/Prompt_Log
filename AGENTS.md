# 저장소 기여 가이드

1. **프로젝트 구조와 모듈 구성**
   - Claude Code·Codex 로그를 날짜별 Markdown·JSONL 리포트로 내보내는 프로젝트.
   - `scripts/export.py`: CLI·Stop 훅 진입점.
   - `scripts/promptlog/`: 파싱, 공통 모델, 집계, 출력, 저장 구현.
   - `tests/`: 테스트.
   - `hooks/`: 플러그인 설정 폴더, 스킬과 설계 문서의 위치 안내.
   - [Python 개발 가이드](dev-stack-python.md): 구현 원칙, 예제, 외부 입력 처리 규칙.
   - [작업 인수 조건](task-acceptance.md): 작업 범위와 검증 근거를 기록하는 양식.
   - [세션 인계](docs/codex-handoff.md): 작업 상태와 실패한 접근·재시도 조건.
   - `Ignore/`: 사용자가 프로젝트나 에이전트에 적용하기전 확인을 위해 임시로 파일을 가져다 놓는 파일 여기 있는 파일은 완전히 무시할것

2. **빌드·테스트·개발 명령어**
   - Python 3.11 이상과 표준 라이브러리만 사용.
   - 별도 빌드 단계 없음.
   - 전체 테스트: `python -B scripts/export.py --selftest`
   - 테스트 상세 실행: `python -B -m unittest discover -s tests -v`
   - 단일 로그 내보내기와 기존 세션 일괄 내보내기 명령어 안내.

3. **코드 스타일과 이름 규칙**
   - 공백 4칸 들여쓰기.
   - 함수·모듈은 `snake_case`, 클래스는 `PascalCase`, 상수는 `UPPER_CASE`.
   - 기존 타입 주석과 데이터 클래스 계약 준수.
   - 형식별 파싱은 어댑터에, 공통 동작은 공통 파이프라인에 배치.
   - 주변 코드 스타일을 따르고 무관한 서식 변경은 피하기.
   - 함수는 30줄 이하로 유지
   - 한 줄 최대 100자

4. **테스트 지침**
   - `unittest` 사용.
   - 파일명은 `test_*.py`, 메서드명은 `test_*`.
   - 동작 변경 시 해당 변경을 검증하는 회귀 테스트 추가.
   - 합성 로그와 임시 디렉터리를 사용하고 원본 로그는 보존.
   - 제출 전 전체 테스트 실행. 수치로 정한 커버리지 기준은 없음.

5. **커밋·풀 리퀘스트 지침**
   - 커밋 제목은 한국어·영어 모두 가능하며 특정 접두사 규칙은 없음.
   - 커밋은 하나의 주제에 집중하고 변경 내용을 구체적으로 설명.
   - PR에는 문제, 변경 후 동작, 호환성 영향, 검증 명령어와 결과 기재.
   - 관련 이슈와 민감한 정보를 제거한 출력 예시 포함.

6. **데이터 취급·아키텍처 제약**
   - 개인 대화 로그, 생성된 리포트, 인증 정보는 커밋 금지.
   - 대화 턴 경계와 재실행 시 동일한 리포트 경로 유지.
   - 내용 절단은 출력 단계에서만 적용하고 정규화된 이벤트는 보존.
   - CLI 실패는 0이 아닌 종료 코드 반환.
   - Stop 훅 오류는 에이전트 실행을 막지 않고 `stderr`에 표시.
