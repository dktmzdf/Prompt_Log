# Tasks

## 1. 저장소 골격

- [x] 1.1 `scripts/promptlog/store.py`를 만들고 design.md §7 스키마와
  `PRAGMA user_version = 1`을 생성하는 함수를 작성한다. 임시 경로에 DB를 만든 뒤
  `sqlite_master` 조회로 5개 테이블과 인덱스가 생기는지 검증하는 테스트를 추가한다.
- [x] 1.2 연결 시 `journal_mode=WAL`과 `busy_timeout`을 설정한다. 같은 DB에 두 연결을
  열어 동시에 쓰는 테스트로 `database is locked` 없이 완료되는지 검증한다.
- [x] 1.3 이미 스키마가 있는 DB를 다시 열어도 오류 없이 재사용되는지 검증하는 테스트를
  추가한다.

## 2. 원본 무손실 적재

- [ ] 2.1 세션 메타(`session` 행)와 원본 줄(`raw_line`)을 적재하는 함수를 작성한다.
  줄 내용을 변경·절단·재정렬하지 않는다. 합성 로그로 적재한 뒤 `raw_line`을 순서대로
  이어붙인 결과가 원본 파일과 바이트 단위로 일치하는지 검증한다.
- [ ] 2.2 JSON으로 파싱되지 않는 줄이 섞인 합성 로그를 적재해, 파서가 건너뛴 줄도
  `raw_line`에 원문 그대로 남는지 검증하는 테스트를 추가한다.
- [ ] 2.3 4KB를 초과하는 도구 출력을 담은 합성 로그를 적재해 절단이 없는지 검증하는
  테스트를 추가한다.
- [ ] 2.4 `session.ingested_lines` 워터마크 기반 증분 적재를 구현한다. 파일에 줄을
  덧붙인 뒤 재적재하면 새 줄만 저장되고, 변경 없는 파일을 두 번 적재해도 줄 수가 늘지
  않는지 검증하는 테스트를 추가한다.

## 3. 이벤트 식별자

- [ ] 3.1 `models.py`의 `Event`에 `source_id` 필드를 추가한다. 기존 필드와 기본값을
  바꾸지 않는다. 기존 `--selftest` 33건이 그대로 통과하는지 확인한다.
- [ ] 3.2 `adapters/claude.py`가 행 `uuid`로 `claude:uuid:{uuid}#{kind}` 형태의
  `source_id`를 채우게 한다. 한 행이 여러 이벤트를 내는 경우(assistant 행의
  `model`/`usage`/본문/`activity`, user 행의 `prompt`/`denial`)에 각각 다른 값이
  나오는지 합성 로그로 검증한다.
- [ ] 3.3 `usage` iterations가 여러 개인 행에서 `source_id`가 충돌하지 않도록 순번을
  덧붙인다. iterations 2개 이상인 합성 로그로 검증한다.
- [ ] 3.4 `adapters/codex.py`가 native 세션은 item `id`로
  `codex:item:{id}#{kind}`를, legacy 세션은 `_line_no`로
  `codex:line:{session_key}:{line_no}#{kind}`를 채우게 한다. `id`를 입력 제외 목록에서
  조정한다. native·legacy 합성 로그 양쪽으로 검증한다.

## 4. 색인 적재

- [ ] 4.1 `event` 테이블 적재를 구현한다. `source_id`를 PK로 `INSERT OR REPLACE`하며
  필터용 컬럼(`kind`, `ts`, `tool_name`, `status`)과 `payload` JSON을 채운다. 합성
  로그로 적재 후 조회 왕복 테스트를 추가한다.
- [ ] 4.2 턴 경계 판정을 `store.py`에 구현한다(프롬프트 이벤트마다 새 턴). 같은 입력에
  대해 `assemble()`이 만드는 턴 경계와 일치하는지 비교하는 동치 회귀 테스트를 추가한다.
- [ ] 4.3 `session_event`와 `turn` 적재를 구현한다. `turn_no`는 세션별로 기록한다.
  첫 프롬프트 이전 이벤트도 조회 가능한 위치에 귀속되는지 검증한다.
- [ ] 4.4 재개 세션 중복 병합을 검증한다. 같은 `uuid`를 공유하는 두 합성 세션을 모두
  적재했을 때 `event` 행 수가 두 세션의 합이 아니라 합집합이고, `session_event`는 두
  행을 모두 유지하며, 같은 이벤트가 세션마다 다른 `turn_no`를 가질 수 있는지 확인한다.

## 5. 파이프라인 연결

- [ ] 5.1 `service.export()`에 적재 갈래를 추가한다. `assemble()`·`present()` 이전의
  `log`를 쓴다. 리포트 경로·파일명·내용이 변하지 않는지 기존 테스트로 확인한다.
- [ ] 5.2 적재 실패가 리포트 생성을 막지 않고 `stderr`로만 보고되게 한다. 적재 함수가
  예외를 던지도록 주입한 상태에서 리포트가 정상 생성되고 종료 코드가 유지되는지
  검증하는 테스트를 추가한다.
- [ ] 5.3 DB 경로가 기본적으로 리포트 루트 아래에 놓이고 `--output`을 따라 이동하게
  한다. 임시 출력 루트를 지정한 테스트에서 기본 위치에 파일이 생기지 않는지 확인한다.

## 6. 재색인

- [ ] 6.1 `raw_line`만으로 색인을 재생성하는 경로를 구현한다. 어댑터를 원본 줄에
  적용하며 원본 파일에는 접근하지 않는다.
- [ ] 6.2 `cli.py`에 재색인 옵션을 추가한다. 색인 테이블을 비운 뒤 재색인하면 원래
  내용이 복원되고, 두 번 실행해도 결과가 같으며, `raw_line`이 한 바이트도 바뀌지 않는지
  검증하는 테스트를 추가한다.

## 7. 전체 검증

- [ ] 7.1 `python -B scripts/export.py --selftest`를 실행해 기존 33건과 새 테스트가
  모두 통과하는지 확인한다.
- [ ] 7.2 실제 세션 하나를 임시 출력 루트에 적재한 뒤, `raw_line`을 이어붙인 결과와
  원본 JSONL 파일을 바이트 단위로 대조해 일치를 확인한다.
- [ ] 7.3 실제 재개 세션 쌍(`26b94bea` ↔ `0ec719f4`)을 임시 출력 루트에 모두 적재해,
  `event` 행 수가 두 세션 합보다 공유 `uuid` 632건만큼 적은지 확인한다.
- [ ] 7.4 `AGENTS.md`의 문서 경로 목록을 갱신한다. 설계 문서가 `docs/`로 이동해 현재
  기재된 루트 경로가 맞지 않는다. 문서에 적힌 경로가 실제 파일과 일치하는지 확인한다.
